package merch;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface dataMerch extends JpaRepository<Merch, Integer> {

    List<Merch> findByTitleContainingOrContentContaining(String text, String textAgain);    
}