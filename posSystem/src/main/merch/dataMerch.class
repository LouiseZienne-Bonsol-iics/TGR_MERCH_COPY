package merch;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface dataMerch extends JpaRepository<merchData, Integer> {

    List<merchData> findByTitleContainingOrContentContaining(String text, String textAgain);    
}